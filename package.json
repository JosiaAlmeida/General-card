{
  "name": "childprocess",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "go": "rmdir /s /q output && mkdir output && node src/index.js",
    "go-cluster": "rmdir /s /q output && mkdir output && node src/cluster.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "puppeteer": "^19.2.2",
    "puppeteer-cluster": "^0.23.0",
    "uuid": "^9.0.1"
  }
}